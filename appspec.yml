
version: 0.0
os: linux
files:
  - source:  /
    destination: /home/ubuntu/be
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: scripts/build.sh
      timeout: 6000
      runas: ubuntu
